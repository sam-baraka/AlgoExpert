using System;

public class MonotonicProgram {
	public static bool IsMonotonic(int[] array) {
		if(array.Count()<=1){
            return true;
        }
        int pointerIndex=1;
        for(int i=0;i<array.Count();i++){
            while(pointerIndex<array.Count()-1){
            if(array[po])
            }

        }
	}
}
